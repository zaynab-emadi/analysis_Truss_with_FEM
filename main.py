import numpy as np


# nodal matrix statically
# it should be a matrix with size of NoN(numbers of nodes) * PD(problem dimension)
NodesElement = np.array([[0, 0],
                         [1, 0],
                         [0.5, 1]])
# matrix of elements
# it should be a matrix with size of NoE(number of elements) * PD(problem dimension)
